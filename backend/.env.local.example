# Copy to .env.local (same folder as server.js) — overrides backend/.env
# Use with local Docker Mongo:  from repo root run  docker compose up -d

MONGO_URI=mongodb://127.0.0.1:27017/realestate

# Optional: turn off IPv4-only mode when using Atlas (if you ever need it)
# MONGO_USE_IPV4=false
